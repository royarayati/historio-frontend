/* eslint-disable */
/* tslint:disable */
// @ts-nocheck
/* prettier-ignore-start */

/** @jsxRuntime classic */
/** @jsx createPlasmicElementProxy */
/** @jsxFrag React.Fragment */

// This class is auto-generated by Plasmic; please do not edit!
// Plasmic Project: wjafXWEvDytFogT7SiMy2v
// Component: dnRUnqur1vWa

import * as React from "react";

import Head from "next/head";
import Link, { LinkProps } from "next/link";
import { useRouter } from "next/router";

import {
  Flex as Flex__,
  MultiChoiceArg,
  PlasmicDataSourceContextProvider as PlasmicDataSourceContextProvider__,
  PlasmicIcon as PlasmicIcon__,
  PlasmicImg as PlasmicImg__,
  PlasmicLink as PlasmicLink__,
  PlasmicPageGuard as PlasmicPageGuard__,
  SingleBooleanChoiceArg,
  SingleChoiceArg,
  Stack as Stack__,
  StrictProps,
  Trans as Trans__,
  classNames,
  createPlasmicElementProxy,
  deriveRenderOpts,
  ensureGlobalVariants,
  generateOnMutateForSpec,
  generateStateOnChangeProp,
  generateStateOnChangePropForCodeComponents,
  generateStateValueProp,
  get as $stateGet,
  hasVariant,
  initializeCodeComponentStates,
  initializePlasmicStates,
  makeFragment,
  omit,
  pick,
  renderPlasmicSlot,
  set as $stateSet,
  useCurrentUser,
  useDollarState,
  usePlasmicTranslator,
  useTrigger,
  wrapWithClassName
} from "@plasmicapp/react-web";
import {
  DataCtxReader as DataCtxReader__,
  useDataEnv,
  useGlobalActions
} from "@plasmicapp/react-web/lib/host";

import { SideEffect } from "@plasmicpkgs/plasmic-basic-components";
import { _useGlobalVariants } from "./plasmic"; // plasmic-import: wjafXWEvDytFogT7SiMy2v/projectModule
import { _useStyleTokens } from "./PlasmicStyleTokensProvider"; // plasmic-import: wjafXWEvDytFogT7SiMy2v/styleTokensProvider

import "@plasmicapp/react-web/lib/plasmic.css";

import projectcss from "./plasmic.module.css"; // plasmic-import: wjafXWEvDytFogT7SiMy2v/projectcss
import sty from "./PlasmicRedirectToInlabLogin.module.css"; // plasmic-import: dnRUnqur1vWa/css

createPlasmicElementProxy;

export type PlasmicRedirectToInlabLogin__VariantMembers = {};
export type PlasmicRedirectToInlabLogin__VariantsArgs = {};
type VariantPropType = keyof PlasmicRedirectToInlabLogin__VariantsArgs;
export const PlasmicRedirectToInlabLogin__VariantProps =
  new Array<VariantPropType>();

export type PlasmicRedirectToInlabLogin__ArgsType = {};
type ArgPropType = keyof PlasmicRedirectToInlabLogin__ArgsType;
export const PlasmicRedirectToInlabLogin__ArgProps = new Array<ArgPropType>();

export type PlasmicRedirectToInlabLogin__OverridesType = {
  redirectToInlabLogin?: Flex__<typeof SideEffect>;
};

export interface DefaultRedirectToInlabLoginProps {
  className?: string;
}

const $$ = {};

function useNextRouter() {
  try {
    return useRouter();
  } catch {}
  return undefined;
}

function PlasmicRedirectToInlabLogin__RenderFunc(props: {
  variants: PlasmicRedirectToInlabLogin__VariantsArgs;
  args: PlasmicRedirectToInlabLogin__ArgsType;
  overrides: PlasmicRedirectToInlabLogin__OverridesType;
  forNode?: string;
}) {
  const { variants, overrides, forNode } = props;

  const args = React.useMemo(
    () =>
      Object.assign(
        {},
        Object.fromEntries(
          Object.entries(props.args).filter(([_, v]) => v !== undefined)
        )
      ),
    [props.args]
  );

  const $props = {
    ...args,
    ...variants
  };

  const __nextRouter = useNextRouter();

  const $ctx = useDataEnv?.() || {};
  const refsRef = React.useRef({});
  const $refs = refsRef.current;

  const styleTokensClassNames = _useStyleTokens();

  return (
    <SideEffect
      data-plasmic-name={"redirectToInlabLogin"}
      data-plasmic-override={overrides.redirectToInlabLogin}
      data-plasmic-root={true}
      data-plasmic-for-node={forNode}
      className={classNames(
        "__wab_instance",
        projectcss.root_reset,
        projectcss.plasmic_default_styles,
        projectcss.plasmic_mixins,
        styleTokensClassNames,
        sty.redirectToInlabLogin
      )}
      onMount={async () => {
        const $steps = {};

        $steps["consoleLogOfInlabUser"] = true
          ? (() => {
              const actionArgs = {
                customFunction: async () => {
                  return console.log(
                    `inlab_user: ${localStorage.getItem("inlab_user")}`
                  );
                }
              };
              return (({ customFunction }) => {
                return customFunction();
              })?.apply(null, [actionArgs]);
            })()
          : undefined;
        if (
          $steps["consoleLogOfInlabUser"] != null &&
          typeof $steps["consoleLogOfInlabUser"] === "object" &&
          typeof $steps["consoleLogOfInlabUser"].then === "function"
        ) {
          $steps["consoleLogOfInlabUser"] =
            await $steps["consoleLogOfInlabUser"];
        }

        $steps["goToInlabLoginIfInlabUserNull"] =
          localStorage.getItem("inlab_user") == null ||
          localStorage.getItem("inlab_user") == undefined
            ? (() => {
                const actionArgs = { destination: `/inlab_login` };
                return (({ destination }) => {
                  if (
                    typeof destination === "string" &&
                    destination.startsWith("#")
                  ) {
                    document
                      .getElementById(destination.substr(1))
                      .scrollIntoView({ behavior: "smooth" });
                  } else {
                    __nextRouter?.push(destination);
                  }
                })?.apply(null, [actionArgs]);
              })()
            : undefined;
        if (
          $steps["goToInlabLoginIfInlabUserNull"] != null &&
          typeof $steps["goToInlabLoginIfInlabUserNull"] === "object" &&
          typeof $steps["goToInlabLoginIfInlabUserNull"].then === "function"
        ) {
          $steps["goToInlabLoginIfInlabUserNull"] =
            await $steps["goToInlabLoginIfInlabUserNull"];
        }

        $steps["consoleLogOfTokenExpirationStatus"] = localStorage.getItem(
          "inlab_user"
        )
          ? (() => {
              const actionArgs = {
                customFunction: async () => {
                  return (() => {
                    const token = JSON.parse(
                      localStorage.getItem("inlab_user")
                    ).access;
                    const payloadBase64 = token.split(".")[1];
                    const decodedpayload = window.atob(payloadBase64);
                    const payload = JSON.parse(decodedpayload);
                    const token_expiration_date = payload.exp;
                    const current_timestamp = Math.floor(Date.now() / 1000);
                    const is_token_expired =
                      token_expiration_date < current_timestamp;
                    return console.log(`is_token_expired: ${is_token_expired}`);
                  })();
                }
              };
              return (({ customFunction }) => {
                return customFunction();
              })?.apply(null, [actionArgs]);
            })()
          : undefined;
        if (
          $steps["consoleLogOfTokenExpirationStatus"] != null &&
          typeof $steps["consoleLogOfTokenExpirationStatus"] === "object" &&
          typeof $steps["consoleLogOfTokenExpirationStatus"].then === "function"
        ) {
          $steps["consoleLogOfTokenExpirationStatus"] =
            await $steps["consoleLogOfTokenExpirationStatus"];
        }

        $steps["goToInlabLoginIfTokenExpired"] = (() => {
          if (localStorage.getItem("inlab_user")) {
            const token = JSON.parse(localStorage.getItem("inlab_user")).access;
            const payloadBase64 = token.split(".")[1];
            const decodedpayload = window.atob(payloadBase64);
            const payload = JSON.parse(decodedpayload);
            const token_expiration_date = payload.exp;
            const current_timestamp = Math.floor(Date.now() / 1000);
            const compare_date = token_expiration_date < current_timestamp;
            return compare_date;
          }
        })()
          ? (() => {
              const actionArgs = { destination: `/inlab_login` };
              return (({ destination }) => {
                if (
                  typeof destination === "string" &&
                  destination.startsWith("#")
                ) {
                  document
                    .getElementById(destination.substr(1))
                    .scrollIntoView({ behavior: "smooth" });
                } else {
                  __nextRouter?.push(destination);
                }
              })?.apply(null, [actionArgs]);
            })()
          : undefined;
        if (
          $steps["goToInlabLoginIfTokenExpired"] != null &&
          typeof $steps["goToInlabLoginIfTokenExpired"] === "object" &&
          typeof $steps["goToInlabLoginIfTokenExpired"].then === "function"
        ) {
          $steps["goToInlabLoginIfTokenExpired"] =
            await $steps["goToInlabLoginIfTokenExpired"];
        }
      }}
    />
  ) as React.ReactElement | null;
}

const PlasmicDescendants = {
  redirectToInlabLogin: ["redirectToInlabLogin"]
} as const;
type NodeNameType = keyof typeof PlasmicDescendants;
type DescendantsType<T extends NodeNameType> =
  (typeof PlasmicDescendants)[T][number];
type NodeDefaultElementType = {
  redirectToInlabLogin: typeof SideEffect;
};

type ReservedPropsType = "variants" | "args" | "overrides";
type NodeOverridesType<T extends NodeNameType> = Pick<
  PlasmicRedirectToInlabLogin__OverridesType,
  DescendantsType<T>
>;
type NodeComponentProps<T extends NodeNameType> =
  // Explicitly specify variants, args, and overrides as objects
  {
    variants?: PlasmicRedirectToInlabLogin__VariantsArgs;
    args?: PlasmicRedirectToInlabLogin__ArgsType;
    overrides?: NodeOverridesType<T>;
  } &
    // Specify variants directly as props
    Omit<PlasmicRedirectToInlabLogin__VariantsArgs, ReservedPropsType> &
    // Specify args directly as props
    Omit<PlasmicRedirectToInlabLogin__ArgsType, ReservedPropsType> &
    // Specify overrides for each element directly as props
    Omit<
      NodeOverridesType<T>,
      ReservedPropsType | VariantPropType | ArgPropType
    > &
    // Specify props for the root element
    Omit<
      Partial<React.ComponentProps<NodeDefaultElementType[T]>>,
      ReservedPropsType | VariantPropType | ArgPropType | DescendantsType<T>
    >;

function makeNodeComponent<NodeName extends NodeNameType>(nodeName: NodeName) {
  type PropsType = NodeComponentProps<NodeName> & { key?: React.Key };
  const func = function <T extends PropsType>(
    props: T & StrictProps<T, PropsType>
  ) {
    const { variants, args, overrides } = React.useMemo(
      () =>
        deriveRenderOpts(props, {
          name: nodeName,
          descendantNames: PlasmicDescendants[nodeName],
          internalArgPropNames: PlasmicRedirectToInlabLogin__ArgProps,
          internalVariantPropNames: PlasmicRedirectToInlabLogin__VariantProps
        }),
      [props, nodeName]
    );
    return PlasmicRedirectToInlabLogin__RenderFunc({
      variants,
      args,
      overrides,
      forNode: nodeName
    });
  };
  if (nodeName === "redirectToInlabLogin") {
    func.displayName = "PlasmicRedirectToInlabLogin";
  } else {
    func.displayName = `PlasmicRedirectToInlabLogin.${nodeName}`;
  }
  return func;
}

export const PlasmicRedirectToInlabLogin = Object.assign(
  // Top-level PlasmicRedirectToInlabLogin renders the root element
  makeNodeComponent("redirectToInlabLogin"),
  {
    // Helper components rendering sub-elements

    // Metadata about props expected for PlasmicRedirectToInlabLogin
    internalVariantProps: PlasmicRedirectToInlabLogin__VariantProps,
    internalArgProps: PlasmicRedirectToInlabLogin__ArgProps
  }
);

export default PlasmicRedirectToInlabLogin;
/* prettier-ignore-end */
